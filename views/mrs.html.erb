<div class="container">
  <h2><%= merge_requests.count %> open Merge Requests</h2>
  <% merge_requests.each do |merge_request| %>
    <%= erb :mr, locals: { merge_request: merge_request } %>
  <% end %>
</div>
